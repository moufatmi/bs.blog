{
  "name": "@beausejourvoyage/blog",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && gh-pages -d dist",
    "update": "git add . && git commit -m 'update' && git push && npm run build && npm run deploy"
  },
  "dependencies": {
    "@astrojs/tailwind": "^5.1.2",
    "astro": "^5.2.5",
    "astro-icon": "^1.1.5",
    "tailwindcss": "^3.4.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
